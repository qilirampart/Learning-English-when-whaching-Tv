# 学习进度与问答记录

## 学习进度
- 已阅读 `run.py`、`config.py`、`app/__init__.py`，理解了入口脚本、配置加载、应用工厂的整体流程。
- 了解了 models 层与 routes 层的关系，以及 `User` 模型、蓝图注册和数据库初始化方法。
- 正在学习 `auth.py` 和 `words.py` 路由，计划继续深入 JWT 认证与翻译服务的实现。

## 问题与回答
1. **`if __name__ == '__main__'`、端口与 `debug`**：解释了直接运行 vs 导入的区别、生产／开发使用的端口以及 `DEBUG` 在不同环境的意义。
2. **配置文件条目**：说明了 `SECRET_KEY`、`DATABASE_URL`、`CORS_ORIGINS` 的默认值和上线时的环境变量覆盖机制。
3. **导入配置的兼容处理**：讲解了 app 工厂如何在导入失败时通过 `importlib` 手动加载 `config.py`。
4. **模型目录与方法**：理解 `models/__init__.py` 的作用、`User.__repr__` 的调试用途以及 `to_dict()` 输出 JSON 友好数据的意义。
5. **蓝图与路由**：明确蓝图就是“模块化路由集”，`@bp.route` 负责将端点暴露给前端。
6. **数据库查询与请求体**：了解 `User.query.filter_by(...).first()` 的含义以及 `request.get_json()` 如何获取前端传来的 JSON。
7. **JWT 与翻译服务定位**：目前知道 JWT 工具为需要认证的路由提供门禁，翻译服务则支撑 `words` 蓝图的单词查询。

## 下一步建议
- 继续阅读 JWT 工具与翻译服务的实现，确认其与路由层的调用关系。
- 完成 `auth.py` 与 `words.py` 的剩余部分，再查看 `learning.py`、`statistics.py`，形成完整的后端流程认识。
